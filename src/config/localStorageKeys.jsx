const projectPrefix = 'FoodBook'

export default {
    auth: {
        rememberMe: `${projectPrefix}:RememberMe`,
        tokens: `${projectPrefix}:Tokens`
    },
    language: {
        current: `${projectPrefix}:Language`,
        supportedLanguages: `${projectPrefix}:SupportedLanguages`
    }
}